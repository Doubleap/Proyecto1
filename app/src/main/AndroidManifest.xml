<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="proyecto.app.proyecto1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Clases.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="AIzaSyB83jgQHcyVq0k2JyL2tQOJMHc56WBgnbw" />

        <activity
            android:name=".CampeonActivity"
            android:label="@string/title_activity_campeon"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MenuPrincipalActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaActivity" />
        <activity android:name=".ListaItemsActivity" />
        <activity android:name=".SummonerActivity" />
        <activity
            android:name=".RankingActivity"
            android:label="@string/title_activity_ranking"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>